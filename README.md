# Backend

This is the Express + MongoDB backend app.

## Install

```bash
npm install
```

## Setup

```bash
cp .env.example .env
```

Update the values in `.env` before starting the server.

## Run

```bash
npm run dev
```

The backend runs on `http://localhost:5000`.
